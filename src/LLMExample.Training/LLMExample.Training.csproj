<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- TorchSharp-cpu bundles both TorchSharp and the correct libtorch native libraries.
         This is the recommended approach for Apple Silicon — it handles native lib
         deployment correctly, unlike the separate TorchSharp + libtorch-cpu-osx-arm64 combo. -->
    <PackageReference Include="TorchSharp-cpu" Version="0.105.2" />
    <PackageReference Include="SharpToken" Version="2.0.4" />
  </ItemGroup>

</Project>
